import {Productos} from './Productos';

export class Mermas {
    id: number;
    categoriasMrm: string;
    unidadesMrm: number;
    causaMrm: string;
    // tslint:disable-next-line:new-parens
    product = new Productos;
    // tslint:disable-next-line:variable-name
    solution_boolean = false;

}

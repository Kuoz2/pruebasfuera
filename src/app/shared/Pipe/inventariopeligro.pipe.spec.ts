import {InventariopeligroPipe} from './inventariopeligro.pipe';

describe('InventariopeligroPipe', () => {
  it('create an instance', () => {
    const pipe = new InventariopeligroPipe();
    expect(pipe).toBeTruthy();
  });
});

import { Inventariogestion2Pipe } from './inventariogestion2.pipe';

describe('Inventariogestion2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Inventariogestion2Pipe();
    expect(pipe).toBeTruthy();
  });
});

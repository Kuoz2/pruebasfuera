import { Comproduc2Pipe } from './comproduc2.pipe';

describe('Comproduc2Pipe', () => {
  it('create an instance', () => {
    const pipe = new Comproduc2Pipe();
    expect(pipe).toBeTruthy();
  });
});

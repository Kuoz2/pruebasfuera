import { MinimarketfilterPipe } from './minimarketfilter.pipe';

describe('MinimarketfilterPipe', () => {
  it('create an instance', () => {
    const pipe = new MinimarketfilterPipe();
    expect(pipe).toBeTruthy();
  });
});

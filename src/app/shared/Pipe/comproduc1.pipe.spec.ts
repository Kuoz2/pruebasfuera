import { Comproduc1Pipe } from './comproduc1.pipe';

describe('Comproduc1Pipe', () => {
  it('create an instance', () => {
    const pipe = new Comproduc1Pipe();
    expect(pipe).toBeTruthy();
  });
});

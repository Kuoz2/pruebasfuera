import {AgregarmasmermasPipe} from './agregarmasmermas.pipe';

describe('AgregarmasmermasPipe', () => {
  it('create an instance', () => {
    const pipe = new AgregarmasmermasPipe();
    expect(pipe).toBeTruthy();
  });
});

import {BuscarselectPPipe} from './buscarselect-p.pipe';

describe('BuscarselectPPipe', () => {
  it('create an instance', () => {
    const pipe = new BuscarselectPPipe();
    expect(pipe).toBeTruthy();
  });
});

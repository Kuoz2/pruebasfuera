import { BusquedaVoucherEmitPipe } from './busqueda-voucher-emit.pipe';

describe('BusquedaVoucherEmitPipe', () => {
  it('create an instance', () => {
    const pipe = new BusquedaVoucherEmitPipe();
    expect(pipe).toBeTruthy();
  });
});

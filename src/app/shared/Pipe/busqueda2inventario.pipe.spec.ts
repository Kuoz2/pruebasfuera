import { Busqueda2inventarioPipe } from './busqueda2inventario.pipe';

describe('Busqueda2inventarioPipe', () => {
  it('create an instance', () => {
    const pipe = new Busqueda2inventarioPipe();
    expect(pipe).toBeTruthy();
  });
});

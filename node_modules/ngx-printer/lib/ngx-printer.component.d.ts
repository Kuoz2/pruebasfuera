import { OnInit, Renderer2, ElementRef, EventEmitter } from '@angular/core';
/**
 * Component used to render content when printed to current window
 */
export declare class NgxPrinterComponent implements OnInit {
    private elementRef;
    private renderer;
    private _renderClass;
    get renderClass(): string;
    set renderClass(value: string);
    /**
     * Display single image
     */
    private _imgSrc;
    get imgSrc(): string;
    set imgSrc(value: string);
    completed: EventEmitter<boolean>;
    constructor(elementRef: ElementRef, renderer: Renderer2);
    ngOnInit(): void;
    /**
     * Attach custom class to element
     */
    private setCustomClass;
    /**
     * Add custom image
     * @param source
     */
    private addImage;
}

import { ElementRef, OnInit, OnDestroy } from '@angular/core';
import { NgxPrinterService } from './ngx-printer.service';
/**
 * A directive to mark and store an HTML-Element as an item which
 * can be printed
 * An id has to be set
 */
export declare class PrintItemDirective implements OnInit, OnDestroy {
    private el;
    private printerService;
    /**
     * Optional name to be shown in an component
     */
    printName: string;
    constructor(el: ElementRef, printerService: NgxPrinterService);
    ngOnInit(): void;
    ngOnDestroy(): void;
}
